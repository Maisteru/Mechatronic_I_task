Git push - używane do publikacji lokalnych zmian na serwerze ogólnym
Git commit- dokonuje zapisu aktualnych zmian w projekcie, nie jest równoważne z "push"(kalsyczny save)
Git pull - pozwala na zaimplementowanie zmian z innego branch'u do aktualnego branch'u
Git stash - pozwala zapisać zmiany przez programistę, ale ich nie zapisuje w branch'u(używane w przypadku problemów zgodnosci kodu lub innych problemów)
Git fetch - pobiera obiekty i tagi z innego repozytorium

UART(Universal Asynchronous Receiver and Transmitter) - interfejs komunikacyjny znajdujących zastosowanie głównie w mikrkontrolerach. 
Jest to układ scalony pozwalający na komunikację w postaci asynchronicznego przekazu i odbioru danych, przy pomocy portu szeregowego.
W budowę tego układu wchodzą dwa konwertery parallel-to-serial(równoległo-szeregowy) odpowiadający za konwersję danych pochodzących 
z komputera i serial-to-parallel(szeregowo-równoległy) odpowiadając za konwersję danych wysyłanych do komputera.

I2C(Inter-Integrated Circuit) - interfejs komunikacyjny działający w sposób synchroniczny, przeciwnie do UART. Oznacza to, że sygnał
zegarowy przesyłany jest między układami. Układy I2C dzielimy na dwa rodzaje: master i slave. Ważny jest fakt, że interefejsy te mają
tylko jedną linie komunikacyjną co ozanacz, że w danej chwili możemy albo odbierać albo wysyłać dane(tryb half-duplex).

Mostek H - układ umożliwiający sterowanie kierunkiem obrotów silnika prądu stałego. Zbudowany on jest z czterech tranzystorów załączając 
jednocześnie odpowiednio dwa z nich wymuszamy przepływ prądu przez silnik. 

Czujniki cyfrowe po wykonaniu pomiaru dane są wysyłane za pomocą interfejsu komunikacyjnego. Czujniki analogowe po wykonaniu pomiaru 
jego wynik w postaci napięcia jest wystawiany na pin czujnika. Wystawione napięcie jest proporcjonalne do odczytane wartości. Czujniki analogowe 
narażone są bardziej na zakłócenia, lecz pozwalają na pomiar z większą częstotliwością.
